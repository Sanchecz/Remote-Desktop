<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!--
      Browser RDP/SSH sessions contain target metadata at INFO and DEBUG.
      Keep only actionable warnings and errors in the infrastructure journal.
    -->
    <root level="WARN">
        <appender-ref ref="CONSOLE" />
    </root>
</configuration>
